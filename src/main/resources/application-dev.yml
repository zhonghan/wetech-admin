debug: true
logging:
  level:
    root: debug
    tech:
      wetech:
        admin:
          mapper: trace
server:
  port: 8080
  servlet:
    context-path: /wetech-admin
spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    druid:
      initial-size: 1
      max-active: 20
      min-idle: 1
      stat-view-servlet:
        allow: true
      test-on-borrow: true
    password: root123456
    url: jdbc:mysql://192.168.0.65:13306/rbac_admin
    username: root
  thymeleaf:
    cache: false